apiVersion: 1

providers:
  - name: FounderLink
    orgId: 1
    folder: FounderLink
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    options:
      path: /etc/grafana/provisioning/dashboards
